// components
import { LoginForm } from '@/components/forms';

const page = () => {
   return (
      <div className='flex items-center justify-center'>
         <LoginForm />
      </div>
   );
};

export default page;
