// components
import { RegistrationForm } from '@/components/forms';

const page = () => {
   return (
      <div className='flex items-center justify-center'>
         <RegistrationForm />
      </div>
   );
};

export default page;
